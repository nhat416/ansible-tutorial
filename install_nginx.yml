---
- name: Install nginx
  hosts: workers
  become: true
  
  tasks:
    - name: update cache
      ansible.builtin.apt:
        update_cache: yes

    - name: Install Apache
      ansible.builtin.apt:
        name: apache2-bin
        state: latest
        update_cache: yes
